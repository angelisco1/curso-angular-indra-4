import { CambiarSimboloPipe } from './cambiar-simbolo-pipe';

describe('CambiarSimboloPipe', () => {
  it('create an instance', () => {
    const pipe = new CambiarSimboloPipe();
    expect(pipe).toBeTruthy();
  });
});
